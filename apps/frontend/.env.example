# Base URL de la API de progreso. Por defecto "/api" (el proxy nginx en
# producción y el proxy de Vite en desarrollo resuelven este prefijo).
# VITE_API_URL=/api

# Destino del proxy de desarrollo de Vite para "/api".
# VITE_PROXY_TARGET=http://localhost:3000
